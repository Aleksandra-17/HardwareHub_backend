"""Description для API документации роутера device_types."""

LIST_DEVICE_TYPES = """
Возвращает список всех типов устройств из справочника.

Каждый элемент содержит:
- id — уникальный идентификатор
- name — название (например, «Ноутбук»)
- code — код (например, «NB-001»)
- category — категория: computing, office, network, other
- description — описание
- deviceCount — количество устройств данного типа (вычисляемое)
"""
